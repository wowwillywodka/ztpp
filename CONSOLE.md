# ztpp — Внутриигровая консоль (GZDoom-style)

Консоль реализована в `src/console.hpp` (`con::` namespace), команды регистрируются в `src/main.cpp`.
Весь текст консоли — ASCII (шрифт игры не умеет кириллицу → показал бы `?`).

## Открытие и управление

| Клавиша | Действие |
|---|---|
| **`** (backquote / ё) | Открыть / закрыть консоль |
| **Esc** | Закрыть консоль (если открыта) |
| **Enter** | Выполнить команду |
| **Tab** | Автодополнение имени команды (+ общий префикс; список вариантов в лог) |
| **↑ / ↓** | Перебор истории введённых команд |
| **PgUp / PgDn** | Прокрутка лога |
| **Backspace** | Удалить символ |

- Пока консоль открыта — ввод движения/огня заморожен, мир продолжает обновляться.
- Ввод символов идёт через `SDL_TEXTINPUT` (учитывает раскладку — печатать можно нормально).
- Команды/история — по физическим scancode (не зависят от рус/англ раскладки).
- В лог консоли сыплются события игры (подбор предметов, лут с трупов) и вывод команд.

## Команды

| Команда | Описание |
|---|---|
| `help` | Список всех команд |
| `clear` | Очистить лог |
| `give <что>` | Выдать оружие/предмет. Варианты: `all`, `weapons`, `items`, `ammo`, или имя предмета (см. ниже) |
| `clearinv` | Очистить инвентарь (вернуться к кулакам) |
| `spawn <враг> [n]` | Заспавнить врага(ов) перед игроком (n до 20) |
| `god` | Бессмертие вкл/выкл |
| `noclip` | Проход сквозь стены вкл/выкл |
| `kill` | Убить всех врагов на этаже |
| `heal [n]` | Восстановить здоровье (без аргумента — полностью) |
| `freeze` | Заморозить/разморозить врагов |
| `floor <n>` | Телепорт на этаж n |
| `ep <n>` | Сменить эпизод (1..) |
| `tp <x> <y>` | Телепорт в клетку (x, y) |
| `pos` | Показать позицию игрока |
| `enemies <on\|off>` | Включить/выключить спавн врагов |
| `blood <on\|off>` | Брызги крови при попадании во врага |
| `transitzt <on\|off>` | ROM-точный рендер переходов: D4A6-ресемпл градиента по питчу + питч-сдвиг стен полной силы (d214). Дефолт OFF (легаси-вид) |
| `camback <on\|off>` | Глаз рендера на 1/8 клетки позади игрока (ROM b9a6, в игре всегда включено). Дефолт ON |
| `playsnd <n>` | Проиграть PCM/DAC-сэмпл #n (поиск импактов) |
| `playfm <патч> [нота]` | Проиграть FM-патч как ноту (поиск синт-SFX) |
| `playsfx <id>` | Проиграть звук по ИГРОВОМУ id (таблица ZT @0xc5eb4: 0x1b=выстрел, 0x6b=смена оружия, 0x67=снаряд, 0x68=попадание) |

### Звук (YM2612 / Nuked-OPN2)
Эффекты воспроизводятся НАСТОЯЩИМ чипом YM2612 (vendored `src/opn2/ym3438.c`): PCM — через DAC-регистр,
FM — патчи GEMS (банк @0x5A804) нотой. Карта событие→id выверена по дизасму (sub_0c60c2/0xc615a). Громкость
и вкл/выкл — меню настроек, стр.4. Музыка (9 секвенций @0x5B0CC) — отдельный проход. Подбор: `playsfx <id>`.

### `give` — имена предметов
`handgun` (`pistol`) · `shotgun` · `rocket` (`rocketlauncher`) · `laser` · `pulse` (`pulselaser`) ·
`grenade` · `mine` · `flame` (`flamethrower`) · `fireext` (`extinguisher`) ·
`vest` · `suit` · `flashlight` · `nightvision` · `scanner` (`bioscanner`)

Спец-аргументы: `give all` (всё), `give weapons` (всё оружие), `give items` (все предметы), `give ammo` (полный боезапас несомого).

### `spawn` — имена врагов
`imp` · `fh` (`formerhuman`) · `sgt` (`sergeant`) · `revenant` · `dog` · `hydaca` ·
`boss1` · `boss2` · `boss3` · `fhsf`

Примеры:
```
give all
give shotgun
give ammo
spawn imp 5
spawn revenant
god
kill
tp 10 14
floor 2
```

## Игровые клавиши (FPS, режим 3) — все раскладко-независимы

| Клавиша | Действие |
|---|---|
| W/S или ↑/↓ | Вперёд / назад |
| A / D | Стрейф влево / вправо |
| ← / → | Поворот · Мышь | Поворот (без инерции, чувств. в настройках) |
| Space или ЛКМ | Огонь |
| Z / X · Колесо мыши | Листать оружие (карусель) |
| J | Прыжок · C | Присед (удержание) |
| N | Noclip · I | God · H | Заморозить врагов |
| V | Дать ВСЁ оружие (тест) |
| F | faithful/DDA · R | Reference-режим |
| Tab | КАРТА уровня (пауза, твоя позиция) · 1/2/3 | Эпизод |
| Esc | Меню настроек · F11 | Фуллскрин · `` ` `` | Консоль |
| Y / U | Враги медленнее / быстрее |
