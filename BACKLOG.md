# ztpp — BACKLOG (что требуется исправить)

Список задач порта Zero Tolerance. Дисциплина: чинить ПО ДИЗАСМУ (не гадать), сверять живьём.
Дизасм: `ztresearchdocs/disasm/annotated/zt/*.asm`. Парный файл тупиков: `dead_ends.md`.
Статус: ☐ открыто · ⏳ отложено · ✅ сделано (ждёт живой сверки) · 🔬 частично.

---

## Инвентарь / предметы / проценты
- ✅ **Пикап огнемёта не отображается на карте** (2026-07-13, ждёт живой сверки) — пикап ct **0x82** (не ct−0x18!).
  `itemBillboardForCt` делал `idx=ct−0x18=106` (вне диапазона) → `return false`. Добавлен спец-кейс ct0x82→**tile 60**
  (ROM-диспетчер `1131e`: entry[106]=хендлер 0x115cc → `movea.l #$1161be` → (0x1161be−0x10E9BE)/0x200=60). Рисуется. Локация теста: ep1/floor3 (21,12).
- ✅ **Бронежилет при подборе даёт 0%** (2026-07-13, ждёт живой сверки) — механика брони корректна (`armor=ammo[3]*10`,
  пикап даёт ammo[3]=10=100%). «0%» был потому, что не-оружие (VEST weapon=false) РАНЬШЕ не рисовало число в карусели.
  Фикс %-дисплея (показ для всех id 1..14) закрыл: жилет полный → «100%». Локация теста: ep1/floor3 (10,4).
- 🔧 diag: добавлен `--findct 0xNN` (скан всех эпизодов/этажей на celltype → печать локаций).
- ☐ **Биосканер не работает** — BIO SCANNER idx1 (ct0x19). Функция предмета не реализована (что он делает — по дизасму диспетча предметов 0x113F4).
- ✅ **Лимит = 100% у расходников-предметов** (2026-07-13, ждёт живой сверки) — кап огнемёта был БАГ 99, ROM @0x11240=0x0a00=10.0;
  исправлен на 10. Все расходники (кап≤10: сканер/жилет/огнетуш/костюм/фонарь/ночник/огнемёт) в кокпите → **% = ammo·100/cap**
  (0..100, шаг 10%). Стволы (кап 99) → count. ⚠ДИЗАСМ-нюанс: оригинал НИГДЕ не рисует «100%» цифрой — кокпит `sub_010a7a`
  показывает СЫРОЙ count `((ammo-1)>>8)+1` (огнемёт=«10»), ID-карта `sub_057f3e` = 8-сегм. бар `(count·8-1)/cap`. «%» = порт-решение (по твоему запросу).
- 🔬 **Проценты в инвентаре отображаются криво** — счётчик на иконке (weapons.cpp drawInventoryHud). Формула % внедрена;
  осталось: расходники-ПАССИВКИ (жилет/сканер/фонарь/костюм/ночник) НЕ в оружейной карусели → отд. HUD-дисплей (ROM семейство 10bd8/10cc6/10dc0/10eac/10f98).
- ✅✅ **ОГНЕМЁТ — ПРИНЯТ ЖИВЬЁМ (2026-07-13: «похоже на то что верно»).** Полный разбор [[zt-flamethrower]]. Фиксы:
  (1) частица летит с аркой (гравитация zVel=−2), в полёте НЕ жжёт, при импакте → урон 1× + НАЗЕМНЫЙ ОГОНЬ (spawnFire);
  (2) звук SFX_FLAME ретриггер ~1/сек (было ~30/сек, no-op гейт); (3) анимация огня — мерцание КАЖДЫЙ кадр (было ÷4) +
  СЖИМАЕТСЯ с возрастом, lifetime 16. ⏳опц: ровная эмиссия 1/кадр (сейчас 2 частицы пачками) — только если визуал «рвано».

## Стрельба / кровь / детект
- 🔬 **Детект врагов — радиус видимости не по дизасму** — «как будто радиус не тот». Сверить конус `167b0`
  (dist²≤0x400 кэп, конус d1) и дальности оружия vs порт `coneTargetEnemy`. Возможно связано с диагональ-LOS ниже.
- ☐ **Кровь на диагональной стене как у квадрата** — при попадании крови в диагональ прилипает как к квадрату,
  а не к тонкой гипотенузе. Кровь-в-стену 0x13cbc/13cbc (spawnBlood think), диагональ-геометрия как в DDA (cellBlockedAt).
- ✅ **[СДЕЛАНО 2026-07-14] Классификация крови ПОЛНОСТЬЮ пересверена по objdef ROM @0xAB0C (receive=think+8) + исчерпыв.
  скан 11 вызовов 0x157ca (5 были СПРЯТАНЫ в code-as-data, текст-grep их не видел → прежняя классиф. НЕВЕРНА).** Итог:
  hit-gib×1 = Sgt/FH/Imp/Dog/FH-SF (0x29/2A/2B/68/69); death-gush×2 = боссы (0x67/6A/6B); БЕЗ крови = Hydaca 0x65, **Revenant 0x66**.
  Правки кода: +Imp +Dog в enemyBleeds, −Revenant (был и в bleeds и в gush), боссы только в gush, gush гейт killedNow. Ошибки
  дизасм-аннотаций записаны в `ztresearchdocs/disasm/annotated/DISASM_FIXES_NEEDED_zt.md`.
- ✅ **[СДЕЛАНО 2026-07-14] Кровь «пшикала» у врага — базовая скорость струи в 16× слабее.** ZT 0x157ca: vel=d3/d4(=норм.вектор
  удара ×256)+rnd → база 1.0·unit кл/кадр (СТРУЯ). Порт имел dirX*0.06 → dirX*1.0. Физика частицы (16 подшагов/гравитация/трение/
  прилипание/лужа/адаптивное старение) уже была верна.
- 🔬 **Детект врагов у диагоналей (квадрат)** — enemyLOS переведён на полуплоскость (сделано), но проверить остаток
  (движение врагов = квадрат = faithful, НЕ трогать; ROM 14858). Возможно связано с «радиус не тот» выше.

## ИИ врагов
- ✅ **[2026-07-14, разобрано] Former Human «слишком часто попадает» — точность УЖЕ фейтфул.** Дизасм fire 18452:
  ролл попадания rnd&0x3FF < порог по СТОЙКЕ+ТЬМЕ: стоя+светло **0x400=100%** (ролла нет, 18488→184a2), стоя+тьма 0x300=75%,
  присед+светло 0x200=50%, присед+тьма 0x17b≈37%. Порт `enemyShotMiss` = ТОЧНО эта модель (0x400/0x300/0x200/0x17b). Частота:
  ROM цикл ~30-55 тик (advance dist>>6+rnd + fire-окно), порт fireCd=55 фикс — НЕ чаще. ⇒ FH 100% попадает по СТОЯЩЕМУ в свете =
  ДИЗАЙН (приседай/прячься в темноте). Не баг. (Опц. будущее: fireCd по дистанции = ROM-точнее вблизи, но это ЧАЩЕ — против жалобы.)
- ✅ **[СДЕЛАНО 2026-07-14, ждёт живой сверки] Revenant — ПАТРУЛЬ + COMBAT переписаны по дизасму (юзер: «combat не соответствует»).**
  ПАТРУЛЬ (нав 0x1ac76): 8 направлений, идёт ПРЯМО пока в центре клетки не упрётся → поворот на СЛЕД. (state+1 mod 8), детерминир.
  (было random). Детект 0x1ac68/0x1ad72: dist≤**0x12c=1.17кл** напрямую ИЛИ ≤**0x180=1.5кл**+LOS (было 2.6кл). walk-check: закрытая дверь блок.
  ⭐ **COMBAT (states 8/9, вскрыт полностью):** НЕ прямой бег к игроку! engage 0x1abe0 = AIM-точка = **игрок + упреждение(скорость) +
  rnd±0x100(±1кл)**, state8, timer=(dist>>7)+rnd&0xf. Цикл: **state8 approach** (рывок ±0x40 к AIM; контакт dist≤0x40=0.25кл → урон
  0x384→2 + звук 0x1b, i-frame гейтит) → достиг aim/timer → **state9 aim** ($35=0xa=10т: движется к aim, ОДИН выстрел на $35==6 =
  hitscan LOS+стелс-ролл(0x400/0x300/0x200/0x17b)→урон 16−(dist·2>>6) ≤2кл, звук 0x1e; заряд-звук на $35==5/4) → $35==0 → ре-engage
  (новая aim). Порт: EC_RANGED_FAST `revEngage` лямбда + states 1(init)/8/9. Верифиц. трейсом (циклит 8↔9, стреляет на t=6, aim wander).
  ⚠ Было: тупой `moveTo(игрок,1.0)` + выстрел по fireCd — совсем не тот ритм «рывок→пауза-прицел→выстрел».
- ✅ **[СДЕЛАНО 2026-07-14, ждёт живой сверки] Revenant — радар + ПАК-АЛЕРТ + подстрел (фидбэк юзера).**
  ✅ **РАДАР-ФИЛЬТР ИСПРАВЛЕН (юзер: «патрульные Revenant/Dog ДОЛЖНЫ быть видны без сканера»).** Дизасм eb5a: враг показывается
  по флагу **бит 0x10 (стоит при СПАВНЕ, objdef+$10=0xdc)** + радиус окна, НЕ по aggro/state. Прежний порт-фильтр `!aggro→continue`
  (mapview.cpp) СКРЫВАЛ патрульных — снят: теперь ВСЕ активные/заспавненные враги в окне видны (верифиц. дампом: красные блипы
  патрульных Revenant'ов). BIO SCANNER (inv.owned[1]) — ДОПОЛНИТЕЛЬНО дормантные маркеры (pendingSpawns). ⚠ Прежний вывод «aggro-фильтр
  верен» был ОШИБОЧЕН (я неверно понял первое сообщение юзера). Плюс 2 гэпа держали его aggro (иначе тоже пропадал):
  (1) **Снят disengage** (`d>4→state0`) — ROM states 8/9 НИКОГДА не возвращаются в патруль (только смерть/стаггер→re-engage),
  мой disengage мигал в state0 → терял aggro → пропадал. Теперь держится (виден, преследует неотвязно = робот).
  (2) **ПАК-АЛЕРТ реализован (ZT 0x1ab6c):** детект ОДНОГО будит ВСЕХ Revenant'ов в 4кл (0x400)→в бой. Триггер = ДЕТЕКТ
  (проксимити), НЕ попадание. Верифиц. трейсом (игрок вплотную к кластеру → 7 разбужено; издалека → 0).
  (3) **Подстрел (hHit 0x66):** Revenant, получив урон, вступает в бой сам (ZT hHit→стаггер→1abd8 re-engage), НО пак-алерт НЕ
  зовёт → соседей не будит ⇒ можно снять по одному СНАЙПИНГОМ ИЗДАЛЕКА (группа спит). «Хит будит всю группу» = было проксимити
  (подошёл стрелять → все в 1.5кл детектят); теперь издалека — только подстреленный.
- ✅ **[СДЕЛАНО 2026-07-14, ждёт живой сверки] FH НЕ открывает двери — гейт по типу.** Дизасм: двери открывают ТОЛЬКО 5 типов
  (b1c4/b202): **Sgt 0x29 / FH-SF 0x69 / Boss1 0x67 / Boss2 0x6B / Boss3 0x6A**. НЕ открывают: **FH 0x2A / Imp 0x2B / Hydaca 0x65 /
  Revenant 0x66 / Dog 0x68** (закрытая дверь их БЛОКИРУЕТ; проходят лишь уже открытую). Порт: `enemyOpensDoors(ct)` + параметр
  `canOpen` в `enemyBlockedAt` (толкает створку только опенер). ⚠ Прежняя память (2026-06-26) «юзер: враги НЕ ходят сквозь двери» —
  если хочешь ВООБЩЕ никого сквозь двери, `enemyOpensDoors` → false (1 строка). Сейчас = дизасм-фейтфул (5 открывают).
- ✅ **[СДЕЛАНО 2026-07-14, ждёт живой сверки] Revenant при патруле шёл «задом» → знак cross в `enemyDirIndex` исправлен.**
  Дизасм 0x1ba2a (селектор ракурса): для directional-врагов (Revenant walk = **6 видов**!) `d4 = (posY−camY)·velX − (posX−camX)·velY`.
  Порт-cross был `ux·vy − uy·vx = −d4` → лево/право виды (dir2/3 бок, dir4/5 боко-фронт) ЗЕРКАЛИЛИСЬ (вбок смотрел не туда).
  Фикс: `cross = uy·vx − ux·vy` (= ROM d4). Фронт/спина (dir0/1) были верны. Чинит ВСЕХ directional-врагов (4/6 видов). Сверено рендером 6 видов.
- ✅ **[СДЕЛАНО 2026-07-14, ждёт живой сверки] Hydaca «тупо бежит на тебя» → WANDER-ИИ (юзер: в MAME ИИ сложнее).**
  Дизасм think 0x14c2a (полная стейт-машина): state0 ПОЛ / state1 ПОТОЛОК / state2 ПАДЕНИЕ / states3-A КЛИМБ (8-напр по стене).
  ⭐Ре-аим **0x14bb4**: aim = **игрок + rnd±1кл** (не прямо к игроку!), суб-таймер $35=dist>>7+rnd&0xf. Пол: краул к WANDER-точке,
  укус dist≤0x64=0.39кл + кулдаун $41==0 (урон 0x2bc→6, cd 10), $35 истёк → НОВАЯ точка (часто с др. стороны = «отвернулся»).
  Потолок: краул + 1/128/тик отцеп (0x14d2c rng&0xfe0==0) → падение. Порт был `moveTo(cam.px,cam.py)` = прямо к игроку. Фикс:
  `hydReaim` лямбда (wander-аим + $35), оба краула к aimX/aimY. Верифиц. трейсом (кружит: 17 aim-точек ±1кл, дист осциллирует
  подход→укус→отворот→снова).
- ✅ **[СДЕЛАНО 2026-07-14] Нокбэк урона СЛИШКОМ СЛАБЫЙ (юзер: Hydaca недостаточно откидывает) → таблица D900.** Дизасм d800 @D89E:
  нокбэк = вектор от источника × **force из @0xD900[индекс·4]+$2** (16×[stun,force], индекс = X>>6 = **16−dmg**), норм ≤0x100,
  затухание **×¾/кадр** (dd46). Порт был `k=0.06+0.004·dmg` = ВТРОЕ слабее. Hydaca(dmg6→idx10)=force81=0.316кл/тик (был 0.084),
  Imp(12→idx4)=126, взрыв(≥14)=130+. Фикс: `KNOCK_FORCE[16]`, `k=force[16−dmgOrig]/256`, декей 0.80→0.75. НОКДАУН (питч в пол)
  только при **stun≥7** = idx≤2 = **dmg≥14** (было dmg≥6, слишком часто). ⚠ Меняет нокбэк ВСЕХ врагов (все были втрое слабы).
- ✅ **[СДЕЛАНО 2026-07-14, ждёт живой сверки] Hydaca климб 3-A — ТОЧНАЯ 8-стейт-машина (юзер: хотим макс. точный порт).**
  Дизасм 0x14f2a-14fe6: 8 climb-стейтов = 4 стены × 2 латерали. ВХОД (ZT 150de/1512e/1517e/151ce, вызов из 14d82 у края клетки):
  wall-check `0x1521e` (celltype LUT==1) + **1/8 rng** (rng&0xe0==0) → climb-стейт по (сторона стены, латераль к aim $38/$3a),
  z-vel $2e=rnd(2..5) знак пол↑/потолок↓. КЛИМБ (15030): фикс. латераль ±0x28 вдоль стены + Z($24) ползёт; потолок(0x18)→st1/пол(−0x18)→st0;
  стена кончилась → СРЫВ (15086: state2 + латер. пуш ОТ стены ±0x28). ПОРТ (actors.cpp EC_HOPPER): states **3-10** = ROM 3-A, таблица
  `HC[8]` (проба-стены/латераль/срыв), per-tick вход `tryClimb` (1/8), падение с латер.пушем + гравитация. ⚠ КЛЮЧ: краул через `hydCrawl`
  (hydStep БЕЗ R-марджина moveTo) — иначе Hydaca стояла 0.26кл от стены и не доходила до края (wd=−1, климб не срабатывал; ZT move 15246
  вплотную). Верифиц. трейсом: st=3/4/7/8/10 задействованы, Z ползёт вверх/вниз, латераль фикс. вдоль стены (не к игроку). 1347 climb-событий.
- ✅ **[СДЕЛАНО 2026-07-14, ждёт живой сверки] Hydaca ЗАСТРЕВАЛА у диагональ+квадрат стены (юзер: в ориг. тоже, но ВЫРЫВАЕТСЯ).**
  ROM wall-check климба `1521e` = **celltype LUT==1** (солидная стена); ДИАГОНАЛИ (icon 2-5) LUT≠1 → Hydaca их НЕ лазит. Порт использовал
  `cellRenderWall`/`cellBlocks` (вкл. диагонали) → лез на диагональ, застревал в climb-петле. Фикс: `hydClimbWall(ct)`=icon 1/12/14 (не диагональ,
  не дверь) в `hydacaWallDir`+continuation. У угла лезет по КВАДРАТНОЙ стене (побег), диагональ игнорит; re-aim wander выводит.
- ✅ **[СДЕЛАНО 2026-07-14, ждёт живой сверки] Hydaca АНИМАЦИЯ ПАДЕНИЯ неверна (юзер: расхождение в анимациях).** ROM draw `1539e` state2:
  по $2e(z-vel)/$36(HP) → **a1**(вниз, вертик 32×64) / **a8**(вверх-срыв, гориз 64×32) / **a10**(мёртв, гориз 64×32). Порт для ВСЕХ падений брал
  DIRECTIONAL `climbDir` (вертик, ракурс крутится). Фикс: `animSt=5` (падение) vs `animSt=4` (лазание); поза по vz/hp: vz>0→a8, vz≤0→a1, hp<0→a10.
  Загружены `A.fallUp`(a8)/`A.fallDead`(a10). Сверено рендером (a8 гориз≠a1 вертик).
- ✅ **[СДЕЛАНО 2026-07-14, ждёт живой сверки] Hydaca ПОЗА ЛАЗАНИЯ — ТОЧНЫЙ per-state выбор (было atan2-аппроксимация).** Разобраны
  ВСЕ 8 веток ROM draw `1539e` (15440-155cc): поза = f(состояние 3-A = стена+латераль, ракурс dx=posX−camX/dy=posY−camY). Каждый state
  выбирает из 3 поз: «близко» (перпендик>латер) → фронт (a3/a4), «далеко» → бок (a1/a2/a6/a7). Пары по латерали: E-краул {a4,a2,a6} ↔
  W-краул {a3,a1,a7} (зеркало). Порт брал ТОЛЬКО atan2 к игроку → игнорил стену+латераль → поза смотрела не туда. Фикс: `hydClimbPose(state,dx,dy)`
  (actors.hpp, таблица 8 веток), climbDir-idx a1=0/a2=1/a3=2/a4=3/a6=4/a7=5. Юнит-тест 8×8 ракурсов ✓. Теперь лазание пиксель-точно.
- ✅ **[СДЕЛАНО 2026-07-14] Hydaca floor-поворот сломался (регрессия): «только вперёд смотрит».** `hydCrawl` считал скорость локально,
  НЕ писал `a.vx/a.vy` → `enemyDirIndex` видел скорость 0 → dir всегда 0 (фронт). Hydaca floor a0 = **4 направления** (walkDirs=4). Фикс: `a.vx=avx; a.vy=avy` в hydCrawl.
- ✅ **[СДЕЛАНО 2026-07-14] ОБРЕЗКА СПРАЙТОВ ВРАГОВ у стен (ВСЕ враги): «половина спрайта».** z-тест reference-рендера (faithful.hpp:1012)
  был `f >= zbuf[x]` — стена ВЫИГРЫВАЕТ равенство → боковая стена «вровень» срезала пол-спрайта. DDA-рендер уже имел фикс (BIAS=0.40,
  «спрайт выигрывает равенство» ZT ef20), но в REFERENCE-режиме используется faithful-версия — БИАС не перенесён. Фикс: `zbuf[x] < f − 0.40` (≈полу-ширина спрайта). Сверено скрин.
- ✅ **[СДЕЛАНО 2026-07-14] Нокбэк игрока: ВАРИАБЕЛЬНОСТЬ ПО СТОЙКЕ + ⚠исправлено направление стойки (было ОБРАТНОЕ).** Юзер: «урон Hydaca
  не откидывает, лишь тряхнуло, в игре ВАРИАБЕЛЬНО» → потом уточнил «в оригинале НЕ роняло на пол». Дизасм d800: стойка модифицирует
  **СЫРОЕ X ДО таблицы** (d80e перед d834), НЕ урон. ПРЫЖОК X×0.75 → X меньше → урон+нокбэк БОЛЬШЕ; ПРИСЕД X×1.5 → меньше, при
  X≥0x400 УВОРОТ (d82a rts). Индекс нокбэка = X_стойка>>6 → force варьируется: Hydaca стоя81/прыжок100/присед16. Нокдаун (питч в пол)
  ТОЛЬКО idx≤2 (взрывы) — Hydaca (idx10) на пол НЕ роняет ✓. ⚠ Прежняя правка «стойка на УРОН ×0.75/×1.5» имела ОБРАТНОЕ направление
  (память неверно прочла d816). damagePlayer переписан: X=(16−dmg)<<6, стойка на X, уворот, force[idx], нокдаун idx≤2, перк бойца 3/4.
- ✅ **[СДЕЛАНО 2026-07-14, ждёт живой сверки] Hydaca ЗАСТРЕВАЛА у диагональ+квадрат УГЛА (юзер: всё равно застревает).** Диагональ-исключение
  (пред. фикс) не хватило: per-tick вход в климб (1/8) слишком агрессивен — у угла Hydaca ПИНГ-ПОНГИТ вверх/вниз по квадратной стене, крауль-окно
  короткое → не успевает выползти (ROM тоже застревает, но крауль-wander успевает вывести). Фикс: КЛИМБ-КУЛДАУН — `a.fireCd=25` после climb→пол/потолок,
  `tryClimb` блокируется при `fireCd>0` → 25-тик окно краула → wander (±1кл, ~¼ в открытую сторону) выводит из угла.

## Двери / разрушаемые стены / взрывы (⭐ VERIFIED 2026-07-13, 3 адверсарных агента — байт-трассы)
**Разрешённая механика** (см. [[zt-destructible-walls]]): ЕДИНАЯ дверная подсистема `$FF0EC6` для 0x06/07/83/84.
0x06/07=двери (проходимы, step-on+враги открывают, обратимо state0); 0x83/84=сплошные разрушаемые стены (class1, враги
открывают обратимо, step-on нет); 0x2D/0x2E=транзитная текстура «открытой створки» (НЕ обломки, по ним нет скана).
**Разрушение в проходимую пустоту (клетка=0 навсегда) = ТОЛЬКО камера-тревога 0x26** (`15c74` 11×11 → `b130/b168` state2,
сохранённый байт=0). Пуля/граната НЕ ломают 0x83/84. Секрет-стены 0x79/7B/7D/7F ломает ТОЛЬКО выстрел (`a6bc`, смена текстуры).

- ✅ **[СДЕЛАНО 2026-07-13, ждёт живой сверки] Пуля игрока НЕ ломает 0x83/0x84.** `damageRay`/`traceMiss` →
  `requestDestruct` ТОЛЬКО для `wallIsSecret` (0x79/7B/7D/7F → смена текстуры a6bc); 0x83/84 пуля не ломает.
- ✅ **[СДЕЛАНО 2026-07-13] Граната/взрыв НЕ ломают НИЧЕГО.** Блок area-destruct 3×3 убран из `explodeAt` (ZT 16294 деструкцию не зовёт).
- ✅✅ **КАМЕРА-ТРЕВОГА 0x26 — ПРИНЯТА ЖИВЬЁМ (2026-07-14: «камера верно отображается и работает»).**
- ✅ **[СДЕЛАНО 2026-07-13] Камера-тревога 0x26 реализована.** `actors.hpp`: `AlarmCam`/`collectAlarmCams`/
  `updateAlarmCams`/`triggerAlarm` (подключено в main.cpp). Детект игрок ≤0x300 (боец4 ≤0x180)+LOS → взвод 33/49 тика
  (писк 0x71/8тиков) → тревога: скан 11×11 `requestDestruct` дверей 06/07 + стен 83/84 → пусто + разбудить+агрить врагов 11×11
  + сирена 0x70/дверь 0x67. Одноразово (state2). Тест: ep2/floor1 (камеры 15,6 / 14,9 / 16,9 / 21,13 / 21,15).
  ✅ **[СДЕЛАНО 2026-07-14, фидбэк юзера] Камера сбивается ТОЛЬКО В ПРЫЖКЕ** (ROM стойка-маска 167e8: флаг камеры +0x4=0x84,
  прыжок→маска 0x80, `0x84&0x80≠0`; стойка 0x40 / присед 0x08 → 0). Камера высоко → целься вверх (прыжком). БАГ был: попадание
  жило только в дебажном `damageRay` (--shootray), а боевой путь `coneTargetEnemy`→`traceMiss` камеру не проверял → сбить нельзя
  вообще. Фикс: `traceCameraHit` (actors.hpp) в `fireSpawn` (weapons.cpp) с гейтом `playerStanceMask()==0x80`.
  ✅ **[СДЕЛАНО 2026-07-14, фидбэк юзера «детект слишком далеко»] Строгий 5-лучевой LOS** (ROM 166a0): дистанция 3кл (verified
  дизасм: 0x300, d7c0=max+min/2, 256/кл — НЕ менять), но LOS теперь = ВСЕ 5 лучей чисты (центр + ±0x1c=±0.109кл смещения точки
  камеры по X/Y) → `cameraLOS`. Убирает срабатывания из-за угла/сбоку (порт был одинолучевой = мягче ROM). Вариант B выбран юзером.
  ⚠ Дистанция детекта = 3кл ПРОВЕРЕНА 5× (юзер помнил ~1кл, но код игры даёт 3; решили ужесточить LOS, а не дистанцию).
  ✅ **[СДЕЛАНО 2026-07-14, фидбэк «должен быть шире»] Ширина спрайта.** Камера = тайл 32×32 → КВАДРАТ на экране. eda0 d4=3S/16
  в 128-колоночном простр. райкастера, дисплей растягивает гориз. ×2 (128→256) → на экране 3S/8 = высота = квадрат. worldFx-путь
  square=true считает width от S (без cwd-×2 декора), поэтому `wFrac16=6` (=hFrac16), а не 3 (был вдвое узкий = старый баг 1:2). Сверено дампом.
  Старое: `damageRay` сбивает камеру в клетке луча (`liveAlarmCamAt`/`destroyAlarmCam` → взрыв+SFX); взрыв тоже валит камеры в радиусе+LOS.
  Камера рисуется билбордом высоко на стене (`pushCameraBillboards` → worldFx), сбитая исчезает. Тест: ep2/floor1, выстрелить в камеру ДО писка → тревоги нет.
  ✅ **[СДЕЛАНО 2026-07-13, сверено дампом] РЕАЛЬНЫЙ СПРАЙТ КАМЕРЫ.** Прошлый вывод «адреса нулевые» был ОШИБОЧЕН —
  у спрайтов просто прозрачный (нулевой) верх, дальше 312/512 ненулевых байт. `eda0` = СТАНДАРТНЫЙ билборд-блиттер
  всех актёров (декор/гибы/пикапы), `a1` = ПРЯМОЙ указатель на пиксели (не anim-дескриптор → потому decodeEnemySprite
  и вернул 0). draw `15d84` даёт **банк объектов 0x10E9BE, tile 19 = 0x110FBE (диод ВЫКЛ, линза красная) ↔ tile 20 =
  0x1111BE (диод ВКЛ, линза тёмная)**, чередуются по `btst #1,-$714a` (бит1 тик-счётчика = `decorFrame`) → блинк период
  4 тика, НЕ зависит от тревоги (draw один во всех состояниях; меняется только при выстреле → взрыв 15dfc банк 0x10EBBE).
  Геометрия из draw-математики: topOff16=−8 (−0x20 в pitch → верх у потолка), hFrac16=6 (3S/8), wFrac16=3 (3S/16, 2:1).
  Реализовано в `pushCameraBillboards` (мигание `(decorFrame()>>1)&1 ? 20 : 19`). Дизасм: enemy_ai.asm 15b1e/15d84/15e4e/15ed2.
  ✅ **Смерть = ВЗРЫВ НА ПОТОЛКЕ, БЕЗ падения** (подтверждено пользователем: в оригинале камера взрывается на потолке и НЕ падает).
  Текущая реализация верна: `destroyAlarmCam` = spawnExplosionA (z=0.7, потолок) + звук 0x8a + мгновенное исчезновение. ⚠ НЕ добавлять
  «падение на пол»: death-think 15dd0 формально имеет короткий дрейф Z (0x10→−0x20 за ~9 кадров), НО актёр уже = спрайт взрыва
  0x10EBBE и деспавнится за 10 кадров → визуально «взрыв у потолка и исчез», не камера на полу.
- ✅ **[СДЕЛАНО 2026-07-13] (cells.hpp maptype 131/132→15, icon14 sht_wall; смоук-дамп ep2/floor5 ок) Рассинхрон icon-слоя: 0x83/0x84 = «невидимая стена».** Порт `cellRenderWall/cellBlocks`(cellIcon) для 83/84 =
  false (maptype16→icon15 «декор») → в 3D не рисуются как стена и не блокируют луч, хотя COLL_CLASS=1 движение блокирует.
  Фикс классификации: 0x83/0x84 должны рисоваться/блокировать луч как стена (game-true icon = разруш.стена, не декор).
- ✅ **[СДЕЛАНО 2026-07-13, ждёт живой сверки — grid-reveal+LOS в explodeAt] Взрыв не бьёт врага, который ещё не видел игрока.** Оригинал: `16294` бьёт ВСЕХ актёров на этаже в радиусе 0x400
  (~4кл) с LOS (`166e8`), БЕЗ фильтра по AI-состоянию → дремлющий враг гибнет; + грид-спавнер `9e1e` материализует
  ЛАТЕНТНЫХ врагов у очага. Порт: `hitEnemy` гейта по state нет, НО спавн гейтится **LOS+5.5кл**
  ([actors.hpp:558-563](ztpp/src/actors.hpp#L558)) → враг за стеной не инстанцирован → взрыв его не находит. Фикс: взрыв
  «проявляет» латентные маркеры у очага. ⚠Также порт `explodeAt` бьёт СКВОЗЬ стены ([actors.hpp:698](ztpp/src/actors.hpp#L698)) — оригинал требует LOS.
- ✅ **[СДЕЛАНО 2026-07-13: wake-агро убрана из explodeAt] «Враги сбегаются на взрыв» — дизасм НЕ подтверждает; НЕ фиксить в эту сторону.** `16294` не рассылает агро/алерт
  (нет `15d18`/`1ab6c`/флага «шум»); таргетинг врага — только дистанция+LOS, звукового агро в ZT НЕТ. Единственный эффект —
  грид-reveal латентных врагов (`9e1e`), что снаружи выглядит как «появились у взрыва». ⚠ Порт СЕЙЧАС будит врагов в радиусе 7
  и наводит на игрока ([actors.hpp:701](ztpp/src/actors.hpp#L701)) — НЕфейтфул. Для reference: заменить wake-агро на грид-reveal (или снять).
- ✅(фейтфул, не баг) **«Враг не может разрушать разрушаемые стены».** Дизасм: враг стены НЕ разрушает — только ОТКРЫВАЕТ
  двери 0x06/07 И 0x83/84 (`b1c4/b202`, обратимо). Перманентное разрушение — только камера-тревога. Мелкий гэп: порт открывает
  врагами лишь cellIsDoor (06/07), а 0x83/84 — нет; низкий приоритет (после классификации icon-слоя выше).
- ✅(разрешено) **«Разрушаемые стены ломаются лишь на расстоянии».** Растворяется: игрок вообще НЕ должен ломать 0x83/84
  выстрелом (см. fix выше). «Расстояние» = дальность хитскана `damageRay` до стены; после снятия пуле-пролома вопрос исчезает.

## Лифт / транзит
- ⏳ **Синий лифта появляется РАНО** — в оригинале синева видна уже КОГДА ПРОШЁЛ ЧЕРЕЗ ДВЕРЬ (не до). Пересмотреть
  дизасм d5ce/сегменты: гейт по прохождению двери, не по достижимости. Связано с [[ztpp-transit-segments-dda]] (edge-P1P2).
- ⏳ **Поездка: сквозь межэтажный синий видно края уровня** — прозрачная середина спана; в ROM кроет стена шахты 0x30
  над/под (§9862, сплошной синий 0xd074). Довести межэтажную заливку.
- ☐ **Звук лифта играет только раз** — при повторном использовании лифта звук кабины не проигрывается. Проверить гейт
  звука (e69e/e6fe: `cmpi #$88,-$58f8; beq skip` — флаг «уже играет» не сбрасывается?). Сбрасывать по завершении поездки.
- ☐ **Лифт двигается при прыжке/приседе перед ним** — в оригинале нет. Сегменты/шахта применяют cam.pitch (jump/crouch),
  а надо только ride-pitch (cabin). (Ранее было снято как «не надо» — вернули в backlog.)

## Диагонали (рендер)
- ☐ **Диагональные стены текстурятся не со всех сторон** — с некоторых углов подхода грань без текстуры. Сверить
  хендлеры c8a8/c910/c97c/c842 (какой texdef/грань на какую сторону) + DDA emitDiag/бокс-обход.

## UI / сообщения / урон
- ☐ **Система сообщений — реализовать по дизасму + правильное место** — HUD-сообщения (подбор/этаж/мало патронов…).
  Разобрать дизасм системы сообщений (текст, тайминг, ПОЗИЦИЯ на экране) и переделать messages.hpp/HudMessages.
- ✅ **С бронежилетом экран НЕ должен краснеть при попадании** (2026-07-13, ✅сверено живьём) — жилет = ПОЛНОЕ
  поглощение (не множитель!). d800: при жилете (-$6f9c≥0) красная вспышка -$6f8e/d98e ПРОПУСКАЕТСЯ (d876 bpl d88e,
  d8e8 bpl d8f6) → в порту `flash` не поднимается при `absorbed` (нокбэк/i-frame/полоска брони остаются).
- ✅ **Стойка влияет на урон** (2026-07-13, ⚠НАПРАВЛЕНИЕ ИСПРАВЛЕНО 2026-07-14) — ZT -$71e6 @d80e модифицирует **СЫРОЕ X** (не урон):
  прыжок X×0.75 → урон+нокбэк БОЛЬШЕ, присед X×1.5 → МЕНЬШЕ/уворот при X≥0x400. Прежняя «прыжок→урон ×0.75» была ОБРАТНОЙ. См. «Нокбэк по стойке».
- ✅ **Перк бойца U-RON снижает урон** (2026-07-14) — -$6fd0==3 → входящий урон ×0.75 (d846), ==4 → ×0.5 (d854), в ветке БЕЗ жилета.
  Реализовано в damagePlayer (fg=playerFighter(): 3→hpDmg×0.75, 4→×0.5) заодно с переписыванием под ROM-модель стойки/нокбэка.

## Управление
- ✅ **Переназначаемое управление + 4 пресета** — СДЕЛАНО (страница CONTROLS, ini, ребинд). Ждёт живой сверки.
